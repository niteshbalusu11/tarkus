export default function Footer() {
  return (
    <footer className="border-t border-slate-200 bg-white px-4 py-5 text-xs text-slate-500">
      <div className="mx-auto flex w-full max-w-7xl flex-col justify-between gap-2 sm:flex-row">
        <span>TARKUS live training assistant</span>
        <span>Human trainer in front. AI synthesis behind.</span>
      </div>
    </footer>
  )
}
